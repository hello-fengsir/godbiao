name: Multi Event Test
on:
  push:
    branches: [main]
  issues:
    types: [opened]
  release:
    types: [published]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Event: ${{ github.event_name }}"
